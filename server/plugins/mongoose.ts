export default defineNitroPlugin(() => {
  // Zero-DB mode: MongoDB connection decoupled. Server runs 100% fast in-memory.
  console.log('⚡ [Jawlah Engine] Running in Zero-DB In-Memory High-Speed Mode');
});
